<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Your Profile</title>
    <link rel="icon" href="/images/head.png" type="image/png">
    <link rel="stylesheet" href="/style/common.css">
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
          theme: {
            extend: {
              colors: {
              }
            }
          }
        }
      </script>
</head>

<body>
    
    
    <div class="w-screen mt-16 h-screen flex items-center bg-red-300 justify-center">
        <h3 class="font-bold text-3xl"><%= message %></h3>
        <form id="updateprofile" method="post"
            class="flex items-center justify-evenly h-full w-1/2 flex-col border-4 rounded-lg bg-black border-blue-700">
            <label for="uname" class="text-white">Username</label>
            <input type="text" id="uname" readonly value="<%= profileDetails[0].username %>"
                class="border-2 border-black  rounded-md text-black bg-gray-200 w-4/12 h-8">
            <label for="fname" class="text-white">Full Name</label>
            <input type="text" id="fname" readonly value=" <%= profileDetails[0].fName  %>"
                class="border-2 border-black  rounded-md text-black bg-gray-200 w-4/12 h-8">
            <label for="uemail" class="text-white">Email</label>
            <input type="text" id="uemail" readonly value="<%= profileDetails[0].email %>"
                class="border-2 border-black  rounded-md text-black bg-gray-200 w-4/12 h-8">
            <label for="nPass" class="text-white">Enter the new Password</label>
            <input type="text" id="nPass" name="newPassword" value="<%= profileDetails[0].password %>"
                class="border-2 border-black  rounded-md text-black bg-gray-200 w-4/12 h-8">
            <label for="nRole" class="text-white">Change Role</label>
            <input type="text" id="nRole" name="newRole" value="<%= profileDetails[0].role %>"
                class="border-2 border-black  rounded-md text-black bg-gray-200 w-4/12 h-8">
            <label for="rFollowing" class="text-white">Enter The Profile Name Whom You don't Want to Follow Any
                More</label>
            <input type="text" id="rFollowing" name="removeFollowing" placeholder="Enter"
                class="border-2 border-black  rounded-md text-black bg-gray-200 w-4/12 h-8">
            <label for="dProject" class="text-white">Enter The Project Name You want to delete</label>
            <input type="text" name="dProject" id="dProject" placeholder="Project Name"
                class="border-2 border-black  rounded-md text-black bg-gray-200 w-4/12 h-8">
            <input type="submit" value="Update Profile Details" class="border-2 border-black rounded-md bg-white p-2">
        </form>
    </div>
    <li></li>
    <li></li>
    <li></li>
    <li></li>
    <li></li>
    <li></li>
    <li></li>
    <li></li>
    <li></li>
    <li></li>
    <script>
        const uName = window.location.pathname.split("/")[3].slice(1);
        const a = document.getElementById("updateprofile")
        a.action = `/user/editProfile/:${uName}`
    </script>
</body>

</html>